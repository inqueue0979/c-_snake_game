#include "MainMenu.h"

int main() {
    MainMenu menu;
    int choice = menu.display();

    switch (choice) {
        case 1: // 게임 시작
            
            break;
        case 2: // 크레딧

            break;
        case 3: // 종료
            break;
    }

    return 0;
}
